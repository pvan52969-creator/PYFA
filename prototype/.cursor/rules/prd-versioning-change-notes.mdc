---
description: 开课等二级菜单需求文档的版本递增与变更说明规范（禁止覆盖旧版）
globs: 参考文档/**/*
alwaysApply: true
---

# 二级菜单需求文档 · 版本与变更说明规范

适用于 `参考文档/2、开课管理/<一级菜单>/<二级菜单>/` 下按菜单拆分的 PRD（md / docx），以及后续同类模块文档。

## 目录结构（母文件夹 = 一级菜单；每版本一文件夹）

```
参考文档/2、开课管理/
  00_设计图资产/
  00_规范与变更说明/
  00_参考数据/
  01_开课设置/
    01_开课时间设置/
    02_校选课程管理/
    03_特殊课程设置/
  02_专业开课/
    01_开课计划/
    02_开课安排/
    03_开课名单/
  05_课程班管理/          ← 03/04 预留选修/特殊开课
    01_课程班/
    02_Teaching Load/
```

一级/二级文件夹带 `NN_` 序号前缀（与侧栏顺序一致）；映射见 `scripts/prd_folder_paths.py`。侧栏有新一级分组时先建序号母文件夹，再建二级菜单文件夹；**每个版本独占一个子文件夹**。

## 现行基线

- 当前已落地基线为 **V2**（命名示例：`开课计划/开课计划20260804V2/`）。
- **禁止覆盖**已有版本文件或版本文件夹内同名文件；再产出时必须升版并新建版本文件夹。
- 若中间版本被废弃（如曾产出 V3 后决定作废），删除该版本文件夹即可；下一版变更说明直接对照**上一有效版本**（例：V2→V4），不必保留空号文件夹。

## 命名

版本文件夹与文件：

```
<二级菜单>/<二级菜单名称><YYYYMMDD><Vn>/
  <二级菜单名称><YYYYMMDD><Vn>.md
  <二级菜单名称><YYYYMMDD><Vn>.docx
```

- 日期：该版本**创建/发布**当日（不必与上一版同一天）。
- 版本号：同一菜单下单调递增 `V1` → `V2` → `V3`…（按菜单各自计数；跳号升版时变更说明仍写实际对照的旧版→新版）。

## 升版时必须同时产出变更说明

模板（必须参考）：

`参考文档/0、模板/厦大马来分校本科教务系统——需求调整变更说明模板（简版）.docx`

变更说明放在**新版本文件夹**内，命名：

```
<二级菜单名称><旧版日期><旧Vn>→<新版日期><新Vn>变更说明.md
<二级菜单名称><旧版日期><旧Vn>→<新版日期><新Vn>变更说明.docx
```

内容结构对齐简版模板：

1. **版本信息**：需求文档名称、上一版→本版、变更日期（**不含**填写人/确认人/确认状态签核栏）  
2. **本次改了什么（总览）**：一行一事；变更类型仅用 **新增 / 修改 / 删除 / 澄清**；列：模块（菜单路径）、功能/位置、变更类型、调整内容、状态  
3. **前后对照**（可选）：总览说不清时再填  
4. **连带影响**（可选）：影响原型/开发/测试时再填  

填写口径：

- 模块：如 `开课管理 → 专业开课 → 开课计划`
- 功能/位置：按钮、字段、列表列、筛选项、抽屉分区/Tab、步骤、弹窗等
- 调整内容：一句话说清「从什么变成什么」；新增写「新增xxx」，删除写「删除xxx」

无实质需求变更、仅重跑脚本且内容与上一版一致时：**不要升版、不要覆盖**。

## Agent / 脚本行为

1. 生成或「再生成」某菜单 PRD 前：落到 `一级菜单/二级菜单/<菜单><日期><Vn>/`；扫描二级菜单下版本文件夹取已有最高 `Vn`，新文件夹用 `V(n+1)` + 当日日期（或显式指定版本；跳号时变更说明的「上一版」取上一**有效**版本）。  
2. **不得**覆盖已有版本文件夹内的 `…Vn.docx` / `…Vn.md`。  
3. 升版时一并写变更说明到**新版文件夹**（至少 md；docx 按简版模板结构生成或由脚本从模板填充）。  
4. 变更说明须对照上一有效版与本版差异（字段表、规则、界面、状态流等），禁止空表交差。  
5. 生成脚本默认拒绝覆盖；显式升版参数才写新版本文件夹。

相关脚本：

- `scripts/prd_folder_paths.py` — 一级/二级菜单序号文件夹映射
- `scripts/generate-prd-20260825-batch.py` / `scripts/generate-sidebar9-prd-20260821.py` — **V2 模板** PRD + 变更说明
- `scripts/prd_submenu_fields.py` — 字段表数据  
- `scripts/generate-submenu-prd-change-note.py` — 变更说明（简版模板）
