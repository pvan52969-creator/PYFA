---
description: 查询条件 + 列表上下布局的前端框架标准（一行最多 4 个条件，按钮固定第一行右侧）
alwaysApply: true
---

# 查询条件 + 列表布局标准

凡「上查询条件、下列表」的页面（排课管理列表、后续新列表、抽屉内同类结构），必须用本标准，不要再发明其它列数、按钮贴底、或按钮下方再堆字段。

## 结构

```html
<div class="card schedule-query-card">
  <div class="schedule-query-row"><!-- 或 query-filter-bar -->
    <div class="schedule-query-fields">
      <div class="schedule-query-field">
        <label for="...">字段名</label>
        <select/input class="input input-sm" id="...">
      </div>
      <!-- 每个条件一格，不要把两个无关字段塞进同一格 -->
    </div>
    <div class="schedule-query-actions">
      <button class="btn btn-primary btn-sm" type="button">查询</button>
      <button class="btn btn-outline btn-sm" type="button">重置</button>
      <!-- 超过一行时：无边框半三角，默认收起第二行 -->
      <button class="schedule-query-fold-toggle" type="button" aria-expanded="false" aria-label="展开筛选" onclick="toggleScheduleQueryMore(this)">
        <span class="schedule-query-fold-caret" aria-hidden="true"></span>
      </button>
    </div>
    <div class="schedule-query-more">
      <!-- 第 5 个条件起 -->
    </div>
  </div>
</div>
<div class="card">列表</div>
```

无 `schedule-query-fields` 包装时，可用 `.schedule-room-filter-grid`：`.srf-item` + 紧跟前四个条件之后（或任意位置，样式会钉到第一行）的 `.srf-actions`。

## 硬性约定

1. **一行最多 4 个查询条件**。按钮组占第 5 列；条件超过 4 个时第 5 个起放进 `.schedule-query-more`。
2. **四列对仗**：条件等宽（`minmax(0, 1fr)`）；上标签、下控件；控件 `width: 100%`。
3. **按钮组在第一行条件后面**（第 5 列），与第一行条件底对齐。
4. **按钮组正下方留空**，第二行及以后条件只占前 4 列，不要排到按钮下面。
5. 日期范围、座位数区间等「一组控件」算 **1 个条件**，占一格。
6. 自动排课等执行按钮用 `.schedule-auto-run-actions`，占位与查询按钮组相同（第一行第 5 列）。
7. 不要用 `flex` + `align-items: flex-end` 把按钮沉到最后一行。
8. **第二行及以下默认收起**。条件超过 4 个时，第 5 个起放进 `.schedule-query-more`（或 `.srf-item` 加 `data-query-more`）；查询/重置右侧用无边框半三角 `.schedule-query-fold-toggle` 展开/收起（无「展开/收起」文案；收起朝下、展开朝上）。不超过一行的筛选栏不加三角。

## 不要改的

- 排时间 / 排教室详情页交互仍遵守 `schedule-time-room-independence`。
- 调课申请抽屉左侧「选择上课节次」也按本标准（一行最多 4 条件，按钮在第一行右侧）。
