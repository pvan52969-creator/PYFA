## 1. 导航与页面壳

- [x] 1.1 侧栏「开课清单」下增加「授课教师替换」导航（`data-page="course-offering-teacher-replace"`）
- [x] 1.2 新增 `#page-course-offering-teacher-replace`：筛选区（学期、开课单位、课程号）+ 已生效课程列表 + 安排明细区 + 最近替换记录区
- [x] 1.3 `goPage` / 列表刷新挂钩，补基础样式（对齐开课清单查询卡 + 明细表）

## 2. 数据与查询

- [x] 2.1 实现 `getEffectiveOfferingSectionsForTeacherReplace(term)`：仅 `submitted` + `schedulingVisible`；专业开课必含，通识已生效可并列
- [x] 2.2 实现安排行视图：从 `groupAssignments` 映射明细列（小组、学时类型、起止周、学时、教师、同时授课）
- [x] 2.3 替换日志 store（内存 + localStorage）与 append/list API

## 3. 整行替换交互

- [x] 3.1 明细行操作「替换教师」：打开既有教师选择器，context 绑定 `sectionId` + assignment row id
- [x] 3.2 确认弹窗文案明确「整行起止周内教师一并替换，不可按周拆分」
- [x] 3.3 确认后整行更新 `teachers`，其它字段不变；刷新明细与课程列表教师摘要
- [x] 3.4 取消路径不写数据

## 4. 确认态与下游

- [x] 4.1 替换成功后重置/失效该 section 相关授课教师确认态，并提示需重新下发/确认
- [x] 4.2 写入替换审计记录（学期、课号、行标识、原/新教师、时间）
- [x] 4.3 冒烟：Teaching Load / 分组工作台对已生效班仍只读，仅本页可改教师

## 5. 演示与收尾

- [x] 5.1 保证至少 1～2 个已生效专业开课带多行安排，便于替换演示
- [x] 5.2 页头/hint 说明适用范围（仅已生效；整行替换）
- [x] 5.3 勾选本 tasks 完成项，必要时补一句联调说明

### 联调说明

1. 侧栏「开课清单 → 授课教师替换」进入；默认学期下应能看到演示课 MPU3113 / OCE101（任务安排已生效）。
2. 点「查看安排」展开明细 →「替换教师」→ 勾选新教师 → 确认整行替换。
3. 检查：该行起止周/学时未变；底部「最近替换记录」有一条；提示需重新确认。
4. 未生效课程不会出现在本页列表；分组工作台对已生效班仍只读。
