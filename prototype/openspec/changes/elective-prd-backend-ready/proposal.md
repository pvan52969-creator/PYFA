## Why

客户已确认原型，但选修开课三菜单 `20260915V1` 是**薄壳**（计划 ~119 行、安排 ~166 行且几乎只有修读范围、名单 ~104 行），对比专业《开课安排》~462 行完整 UI 树 + tip→BR。缺 GE开课 / ME开课 / 一键复制 / 导入 / 生效 / 删除、学分人次与 Quota tip 规则等，**不能交给开发**。必须按「完整 UI 树 + 次级面板 + tip/alert→BR」标准重写。

## What Changes

- **重写**选修开课三菜单 PRD（计划 / 安排 / 名单）至与《开课安排》同级可交付标准；md + docx + 变更说明简版 V3。
- **版本策略（默认）**：在现行正式链 `20260915V1` **同文件夹覆盖重写**（薄版作废，不保留空壳 V1）；变更说明写明「薄版作废 → 完整重写」。若你改口升版则改为 `20260915V2` 并删/保留旧 V1 另议。
- **真源**：`page-course-ge-offering-quota`、`page-course-offering-ge`、`page-course-ge-offering-roster` + `app.js` 可见按钮 / tip / alert；禁止编造；未写明处标 UC。
- **禁止**把专业开课已拍板口径（撤回/退回等）默认套到选修，除非原型可见且一致。
- 原型代码本变更**只读**，不改交互。

### 已知漏项（非穷尽，实施时全量走查）

**选修开课安排**（你点名的按钮所在页）

| 可见能力 | 现行 V1 |
|----------|---------|
| GE开课 / ME开课（随结果页签切换） | 缺失 |
| 一键复制 | 缺失 |
| 导入（含模板/仅未生效可覆盖 tip） | 缺失 |
| 生效 / 删除 / 导出 | 缺失 |
| 一键同步共同授课（师资步） | 缺失 |
| 两步条 Course Setting / 师资安排 + 列表列差异 | 缺失 |
| 顶部计划结果卡 GE/ME + 进度 | 缺失 |
| 修读范围 / 可选批次（旧稿有） | 有，须保留并校对 |

**选修开课计划**（你点名的文/商/理、ME Quota 卡）

| 可见能力 | 现行 V1 |
|----------|---------|
| 文/商/理学分人次卡 + tip 拆分规则 | 缺失/极简 |
| 学院明细折叠表 | 极简 |
| GE 配额表全列（含课程组quota tip、进度） | 极简 |
| ME 需求卡（人数、预计 Quota tip、批次明细） | 标 UC，未写规则 |
| ME 配额表 + 设置弹窗 + 保存/重置/导出 | 缺失 |

**选修开课名单**：统计卡 tip、查询全集、行操作、与选课应用边界 — 须全量补。

## Capabilities

### New Capabilities

（无 — `skip_specs: true`，文档重写，不改产品行为规格。）

### Modified Capabilities

（无）

## Impact

- 文档：`参考文档/2、开课管理/03_选修开课/` 下三菜单 `20260915V1/`（默认覆盖）
- 对照标杆：`开课安排20260915V5` / 历史 `offering-arrangement-prd-complete`
- 过程债（只读参考、不归档）：`elective-module-gap-audit`、`ge-demand-credit-distribution`、`ge-quota-by-g-type`
- 读者：产品 / 研发 / 测试 / AI 后端
