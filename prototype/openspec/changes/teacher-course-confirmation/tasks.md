## 1. 数据模型与聚合

- [x] 1.1 在 `app.js` 增加教师确认包存储（`teacherId + termCode`、状态 draft/pending/confirmed/disagreed、课程行快照、诉求与历史）
- [x] 1.2 确认方式字段：`teacher` / `admin-proxy`；代确认记录操作人与时间
- [x] 1.3 实现从专业开课 section 师资安排聚合「教师 × 学期」课程行；Total Student No. 取课程人数上限
- [x] 1.4 实现 section「教师确认齐套」判断：当前指派教师均 `confirmed`（含代确认）且快照匹配；安排变更波及教师时使确认失效
- [x] 1.5 注入演示数据（未下发 / 待确认 / 教师已确认 / 管理端代确认 / 不同意+诉求）

## 2. 教务确认管理页与入口

- [x] 2.1 `index.html`：分组工作台视角 Tab 增加「授课确认」；新增教务确认管理页/面板结构
- [x] 2.2 `app.js`：`switchOfferingGroupingSidebarView` 支持授课确认视图（教师列表、状态、下发/重新下发、代确认、查看诉求）
- [x] 2.3 Teaching Load / `#modal-offering-teacher-term-assign`：点击教师名跳转到授课确认管理并聚焦该教师
- [x] 2.4 下发 / 重新下发动作：写入 `pending`、更新快照；重新下发保留诉求与代确认历史
- [x] 2.5 管理端「代确认」：二次确认后写入 `confirmed` + `admin-proxy`，列表/详情展示「管理端代确认」标记

## 3. 教师确认页（系统内）

- [x] 3.1 新增教师确认页（按原确认函信息字段排版确认内容；含 Total Teaching Hours 等摘要；**不发邮件**）
- [x] 3.2 提供「确认」与「不同意」；不同意必填诉求后写入 `disagreed`
- [x] 3.3 原型用演示角色/教师切换模拟当前教师身份

## 4. style2 生效门槛与进度

- [x] 4.1 style2 列表展示每课教师确认进度（已确认数 / 指派教师数；可区分代确认）
- [x] 4.2 「生效」增加齐套校验：未齐套则拦截并提示未确认/不同意教师
- [x] 4.3 更新 style2 按钮 title / 二次确认文案，明确需先完成授课确认

## 5. 样式与验收

- [x] 5.1 `styles.css`：确认内容版式、教务列表与 Tab、进度指示、代确认标记样式
- [x] 5.2 手测闭环：下发 → 教师确认 → 生效；不同意 → 看诉求 → 改安排 → 重新下发 → 再确认 → 生效
- [x] 5.3 手测代确认：管理端代确认 → 标记可见 → 计入齐套 → 可生效
- [x] 5.4 手测入口：分组 Tab「授课确认」、Teaching Load 点教师名跳转
