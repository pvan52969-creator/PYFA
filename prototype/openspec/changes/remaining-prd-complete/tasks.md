## 1. 审计固化

- [x] 1.1 将 11 菜单缺口写入本 change 的 `AUDIT.md`（严重度/缺失 UI/tip/错误类型），验证：与 explore 审计表一致且含开课安排「已完成」备注
- [x] 1.2 确认落档=覆盖 V2（除非用户点名升版），验证：design/proposal 策略一致；用户已确认「按提案实现」=覆盖 V2

## 2. P0 补全

- [x] 2.1 覆盖重写《开课计划》完整 UI/字段/BR（含修改抽屉全量、工具栏、Prog.Batch tip），验证：表单 FD 显著多于 5；docx 黄底
- [x] 2.2 覆盖重写《开课名单》（分组工作台学生模式完整矩阵 + 一键分配 tip→已明确 BR），验证：不再写「无工作台」且工作台列对齐原型 thead
- [x] 2.3 覆盖重写《校选课程管理》（D02 全字段 + 批量/新增弹窗 + 列表列），验证：D02 不再零字段
- [x] 2.4 覆盖重写《特殊课程设置》编辑抽屉剩余字段（学分/学时/名额/选课/是否排课等），验证：表单 FD 覆盖抽屉可见项
- [x] 2.5 覆盖重写《授课确认管理》（宽表 + 批量发送/代确认 + Groups tip BR），验证：列表列与 JS 表头对齐
- [x] 2.6 覆盖重写《授课确认（教师端）》（Pending/History + 同意/不同意弹窗字段），验证：不再只有 5 FD

## 3. P1 补全

- [x] 3.1 覆盖补全《Teaching Load》（维度页签 + 行展开明细 + tip BR），验证：含周/学期/学年与展开列
- [x] 3.2 覆盖补全《授课教师替换》（批量替换弹窗 + 列表缺列），验证：UI 树含 batch modal

## 4. P2 小对齐

- [x] 4.1 《课程班》《排课计划》《开课时间设置》轻量对齐（分列/入口/备注与现行 UI），验证：变更说明仅列真实差异

## 5. 脚本与交付

- [x] 5.1 扩展批跑 `SKIP_BATCH_MENUS` 覆盖本批已完整菜单，验证：对 skip 菜单再跑不会覆盖
- [x] 5.2 写 `DELIVERY.md` 列出每菜单路径与抽检结果，验证：文件存在且 P0 六份均勾选
