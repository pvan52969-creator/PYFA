## 1. CSS 等宽栅格

- [x] 1.1 去掉 Teaching Load（含分组页副本）不等宽 `grid-template-columns` 与 `course-time-query-actions { grid-column: auto }`，改回 6 列等宽且查询行整行靠右；浏览器打开 Teaching Load，确认 6 个字段等宽、查询/重置在下一行靠右
- [x] 1.2 将 `.tcc-admin-filters` 改为 6 列等宽 grid，展开区同样 6 列，字段控件 `width: 100%`；浏览器打开授课确认管理，确认收起第一行 6 个等宽字段、按钮行靠右

## 2. 补满第一行字段

- [x] 2.1 课程班：把「课程组名称」移到收起第一行；展开后核对除最后一行外每行 6 个
- [x] 2.2 教师替换：把「开课类型」「上课专业」移到收起第一行；展开后核对最后一行才不足 6 个
- [x] 2.3 排课计划：把「课程组」「学时类型」移到收起第一行；展开后核对最后一行才不足 6 个
- [x] 2.4 授课确认管理：把 Status 移到收起第一行；搜索/重置/展开单独一行靠右

## 3. 教师端同一行

- [x] 3.1 将教师端「导出」并入 `tcc-letter-batch-bar`（授课确认在左、导出靠右），去掉单独的 `tcc-portal-list-toolbar`；Pending 与 History 都核对该行只有一排操作、导出在右

## 4. 验收

- [x] 4.1 浏览器按用户点名页走一遍：课程班、Teaching Load、授课确认管理、教师端、教师替换、排课计划；确认等宽、第一行 6 个、导出与授课确认同一行
