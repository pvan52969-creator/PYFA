## 1. 共用展示入口

- [x] 1.1 增加排课课组展示函数（复用 `formatMajorOfferingGroupName`），带人数时为 `课程班名称（短名）（n）`；用 `Financial Accounting` + `A组` + 45 手测得到 `Financial Accounting（A组）（45）`
- [x] 1.2 确认 `formatScheduleCourseCode` 对 `ACC101` 输出学校课号；列表渲染课号、课名、课组都走这套入口，空值仍为「—」

## 2. 演示数据

- [x] 2.1 改冲突查询等 `CTQ-*` / `Query Demo` / `教师时段演示组` 种子为 `ABC123` + 英文课程班名称 + 短名 `A组`；全局搜旧桩无用户可见残留
- [x] 2.2 更新 `CTQ-TS1` 等硬编码挂钩（含补课 Pending 给李明补节次）到新课号；刷新后补课 Pending 仍有李明停课行且三项为学校格式

## 3. 列名与列表

- [x] 3.1 排课/调课列表表头：课号→Course Code、课程/课名/课程名称→课程班名称、上课小组→课程组（含补课 Pending、冲突查询、自动排课、导出 CSV）；打开补课 Pending 三列标题与取值符合规格
- [x] 3.2 筛选框 placeholder、卡片字段标签中这三项同步改名；抽查筛选仍按 code/name/group 工作
- [x] 3.3 课程组列过长时用现有 ellipsis，不把补课 Pending 表撑乱

## 4. 排时间 / 排教室

- [x] 4.1 排时间课表卡片/侧栏的课号、课名、课组走同一格式；拖排、右键菜单与改前一致
- [x] 4.2 排教室课表卡片同样只改这三项展示；锁定/待定/撤回与改前一致

## 5. 回归

- [x] 5.1 打开专业开课任务安排，确认开课列名与数据未改；培养方案 TAB3 课程组未改
- [x] 5.2 浏览器走补课 Pending、课表冲突查询、按教师排：三项均为学校格式，无 `CTQ-TS1` / `Query Demo` / `教师时段演示组`
