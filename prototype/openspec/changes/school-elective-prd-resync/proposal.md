## Why

「校选课程管理」现行 PRD（`20260901V3`）字段与原型 `page-school-elective-courses` 不对齐：修读范围被写成单一「不可选专业」，实际是「类型（可选/不可选/不限）→ 再选专业」；列表/摘要里的专业字段已统一为「所属专业」，PRD 仍写「开课专业」。需按现行原型严谨重写该版文档。

## What Changes

- **仅文档**：按 PRD 模板 V3.1 **覆盖**同版本文件夹 `校选课程管理20260901V3/`（用户明确授权；非常规升版）。
- 同步覆盖同目录变更说明（仍对照上一有效版 `20260821V2` → `20260901V3`），总览写入本次字段纠偏。
- 修读范围：删除「不可选专业」单字段表述 →「类型」+「专业」。
- 「开课专业」→「所属专业」（列表列、修读范围摘要）。
- 对照原型补全界面树与字段；未知边界标（未确认）。
- **不改** `app.js` / `index.html`。

## Capabilities

### New Capabilities

- （无）

### Modified Capabilities

- （无）已设 `skip_specs: true`。

## Impact

- 文档：`参考文档/2、开课管理/01_开课设置/02_校选课程管理/校选课程管理20260901V3/`（覆盖）
- 脚本：更新批量脚本中校选菜单数据；`--allow-overwrite --only 校选课程管理`
- 非目标：改原型；升 V4；归档旧 change `school-elective-offering-programme`
