## 1. L1 共同授课规则

- [x] 1.1 放宽共同授课校验：允许同课号不同教学班；仍要求 ≥2 个教学班且至少一名相同任课教师；验证同课号两班可确认，不足两班仍被拒
- [x] 1.2 调整共同授课同伴/状态展示，使同课号多班可区分（课号 + 教学班/班标识）；验证同号两班均出现在同伴摘要或等价 UI
- [x] 1.3 清除共同授课标记时同步清空该标记下全部联动组并提示；验证清除后联动数据与「联动」列为空

## 2. L2 数据与校验

- [x] 2.1 为 `groupAssignments` 行确保稳定 `assignmentId`（缺则补）；验证新建/存量行可被引用
- [x] 2.2 在学期开课 store 增加 `sharedTeachingSlotLinks`（或等价）CRUD：创建/查询/按 link 解除/按 assignment 或 section 查找；验证读写后刷新仍在
- [x] 2.3 实现绑定校验：仅周次、周学时一致可绑；不一致返回明确错误文案；验证教师/场地不同仍可绑，周次或周学时不同不可绑
- [x] 2.4 实现自动解除：周次/周学时变更致不一致、删行 → 整组解除并 toast/提示；验证提示含联动组标识与原因

## 3. 联动排课安排界面

- [x] 3.1 新增「联动排课安排」modal/抽屉：展示共同授课组内各教学班安排表、多选、建立联动、已建组列表与解除；验证可完成绑/解全流程
- [x] 3.2 共同授课弹窗已设列表增加「管理联动安排」入口（有 L1 可用）；验证点击进入正确共同授课组范围
- [x] 3.3 分组页学时安排区增加「联动排课安排」入口（无 L1 隐藏或禁用并说明）；验证有/无共同授课两种状态

## 4. 分组表展示与 teaching load

- [x] 4.1 学时安排表增加「联动」列（与「同时授课」区分）：显示 `🔗 #n` 与 tip（组内摘要）；未联动为 —；验证绑后刷新可见、解后清空
- [x] 4.2 Teaching load / 等价课时展示：同一联动组内同一教师只计一次；验证两行同师去重、不同师仍可绑

## 5. 演示与回归

- [x] 5.1 准备演示数据：同课号两班共同授课 + 至少一组周次周学时一致的可联动行；验证打开联动页可一键演示绑/解
- [x] 5.2 回归：不同课号共同授课仍可用；本课内「同时授课」行为不变；未设共同授课无法裸绑
