## 1. 原型：替换副作用

- [x] 1.1 在 `applyOfferingTeacherReplacePending` 中移除「确认未齐套则 `resetMajorOfferingTaskArrangementToDraft`」分支，并改成功 alert 为保生效 + 需重确认口径；验证：单行替换后安排仍已生效、提示无「打回草稿」
- [x] 1.2 在 `applyOfferingTeacherReplaceBatch` 做与 1.1 相同的副作用与文案调整；验证：一键替换后安排仍已生效、提示无「打回草稿」
- [x] 1.3 确认替换后仍对进出教师调用确认包同步，且离课教师本课行从待确认移除、留课/新教师可再下发；验证：授课确认管理/教师端待办与汇总为部分确认或未确认符合预期
- [x] 1.4 检查演示/migrate 纠偏逻辑，避免把「替换造成的已生效+未齐套」再次打回草稿或强制补齐；验证：替换后刷新页面状态不被纠偏吞掉

## 2. 原型：排课计划入池

- [x] 2.1 修改 `isSectionEligibleForSchedulingPlan`：入池只要求安排已生效（及停课排除等既有非确认条件），去掉授课确认齐套判断；验证：已生效但确认未齐套的开课出现在排课计划
- [x] 2.2 更新排课计划页 subtitle、toolbar hint、空态文案及 HTML 注释，去掉「须授课确认完成/齐套」表述，改为以已生效为准；验证：页面可见文案与规则一致
- [x] 2.3 回归共同授课/联动：确认齐套不再作为 cohort 入池门槛，安排与联动完备性校验仍保留；验证：仅确认未齐套不挡共同授课入列，缺安排/联动仍不入列

## 3. 文档

- [x] 3.1 升版《授课教师替换》PRD：改写 BR001（保生效、确认可回退、受影响重下发、离课不下发），并注明「确认生效」门槛不变；验证：新版本文件夹 + 变更说明（简版 V3）
- [x] 3.2 升版《排课计划》PRD：入列条件改为以安排已生效为准、不以确认齐套为门槛，并同步页头/hint 口径；验证：新版本文件夹 + 变更说明（简版 V3）
- [x] 3.3 视需要在《开课安排》或《授课确认管理》加交叉引用/短澄清；验证：无与本拍板矛盾的「排课须确认齐套」残留表述（本 change 触及范围内）
