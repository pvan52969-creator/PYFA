## 1. 继承判定

- [ ] 1.1 扩展 `getAdjustmentMakeupInheritedReason`：对勾选值做 `expandAdjustmentSlotVal` 后收集全部停课；类型不一致返回冲突明细（各记录的原因类型）。验证：两条不同类型停课 `ok === false` 且 message 含分批申请
- [ ] 1.2 类型一致时：事由去重合并、附件按文件名并集，不再只取第一条。验证：同类型不同事由返回的 `reason` 包含两条文案

## 2. 抽屉展示

- [ ] 2.1 冲突态：`syncAdjustmentMakeupReasonFromCancel` 不清空成无解释空白；原因类型只读框 + hint 说明不一致，事由区可见摘要。验证：抽屉内勾选混类型后右侧能读到冲突说明
- [ ] 2.2 成功合并态：只读展示合并后的事由与附件并集；控件仍锁定。验证：同类型两条停课勾选后事由区同时出现两段文字且不可改类型

## 3. 入口与提交

- [ ] 3.1 补课页 Apply：已勾选混类型时仍打开抽屉并进入冲突态（可先 toast/alert）。验证：列表混选 Apply 后抽屉打开且不能当成已同步单一类型
- [ ] 3.2 提交：混类型继续拦截；同类型合并后的事由/附件写入申请。验证：混类型提交 alert；同类型不同事由可提交成功

## 4. 回归

- [ ] 4.1 调课/停课申请仍可自选原因类型并填写事由。验证：非补课抽屉原因下拉可改
- [ ] 4.2 排时间、排教室详情未改。验证：从侧栏进入按入学批次排、按时间排教室，详情交互与改前一致
