## 1. 对照与锁定

- [x] 1.1 通读修读范围抽屉 / 列表表头 / 开课默认抽屉，确认字段清单含：类型+专业、所属专业、可选学生类型、不可选年级学期、冲突/先修；验证与原型一致
- [x] 1.2 锁定目标为覆盖 `校选课程管理20260901V3`（用户授权）；验证将使用 `--allow-overwrite`

## 2. 覆盖产出 PRD V3

- [x] 2.1 按 V3.1 重写正文 md（覆盖）；不得出现独立维护字段「不可选专业」；不得用「开课专业」指列表/摘要专业列；须有类型+专业；验证 grep 通过
- [x] 2.2 覆盖生成同版 docx；验证文件存在
- [x] 2.3 覆盖变更说明（V2→V3 文件名）；总览含类型+专业、所属专业纠偏；本版 PRD 模板=V3.1；验证对象 ID 可在 PRD 中找到

## 3. 收口

- [x] 3.1 抽检用户点名两点已纠正；验证变更说明与 PRD 一致
- [x] 3.2 确认本 change 未改 `app.js` / `index.html` 业务逻辑
