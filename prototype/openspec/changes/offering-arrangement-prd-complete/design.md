## Context

- 见 `proposal.md` Why：现行 `开课安排20260902V2` 严重缩水。
- 真源约定（延续 backend-ready）：**可见 UI 文案 + tip `data-tip` / aria 文案** → 标「已明确」；未在原型出现的校政/权限 →「未确认」，禁止编造。
- 原型主入口：`page-course-major-offering-task-style2`（列表 2 步）→ `page-offering-grouping`（沉浸分组壳）及若干 modal。
- 水印仍写「V8」的是历史生成残留；落档以文件夹版本号为准（本变更产出 **20260903V3**）。

### 已核对的缺口清单（相对 V2）

```
P01 开课安排列表
├── 行展开「开课任务安排」面板（只读预览 + 前往修改）  ← V2 未写
│     列：序号/课程组/学时类型/起止周/周学时/总学时/教师/
│         同时授课(!)/联动安排(!)/场地类型/教室偏好(!)
├── 授课确认弹窗 modal-teacher-course-confirmation-section  ← V2 未写
└── （查询/列表主列/生效撤回等 — V2 有骨架）

P02 分组工作台 page-offering-grouping
├── 侧栏动作：分组 / 重置 / 学时类型设置 / 复制     ← V2 未写
├── 小组树（人数上限、✎ 改名）                   ← V2 未写
├── 安排详情区：配置共同授课 / ✎ ⧉ 行操作         ← V2 未写
├── 安排表字段（含 tip 规则）                    ← V2 仅 FD030–032
├── Teaching Load（按学期等）嵌入区              ← V2 未写
└── 课时详情 modal-offering-teacher-term-assign
      · No. of Groups (!) / Latest Evaluation (!) ← V2 未写
```

### tip 规则摘录（须写入 BR，口径=原型文案）

| 主题 | 原型要点（已明确） |
|------|-------------------|
| 同时授课 | 须手动选是/否（新增默认空）；多组+是→合班同时授课学时只计一次；多组+否→保存后拆成每组一条 |
| 联动安排 | 共同授课组内绑定联动组；排课同时间同教室（开课侧不强制教室相同）；仅要求周次、周学时一致；可查看明细 |
| 教室偏好 | 仅排课参考，以排课安排为准 |
| No. of Groups | 该教师本课参与小组数去重；不论是否同时授课；不是课程总组数 |
| Latest Evaluation | 期中/期末 Latest；按课号汇总不按组；**评教未接入=占位**（须标演示/未确认接入） |
| 学时类型设置 | 已安排教师后不可改；需先删全部教师安排（alert 文案） |

## Goals / Non-Goals

**Goals**

- 一份可交付后端/测试的完整开课安排 PRD（界面 ID 齐全、字段不漏 tip 列、功能与 BR 对齐 tip）。
- md 与 docx 章节一致；改动处黄底、字体与模板一致。
- 变更说明对照 V2→V3，对象 ID 可追溯。

**Non-Goals**

- 不改原型/业务代码。
- 不重写其它 11 个菜单 PRD（除非连带对照短文路径）。
- 不产出 OpenAPI；不发明 RBAC。
- 不把「授课确认（教师端）」整菜单并入本文（本菜单只写安排侧入口弹窗与状态列）。

## Decisions

1. **覆盖 V2（用户 2026-09-03 已确认）**  
   - 直接覆盖 `开课安排20260902V2/` 内 md/docx/变更说明。  
   - 备选升版 V3 已否决。

2. **界面拆分（建议 ID，实现时可微调但须稳定引用）**  
   - `P01-X01` 列表行展开 · 开课任务安排  
   - `P01-M02` 授课确认（安排侧）  
   - `P02-A01` 侧栏动作区；`P02-T01` 小组树；`P02-L01` 安排详情表；`P02-M01` 配置共同授课/联动；`P02-W01` Teaching Load；`P02-M02` 课时详情  
   - 备选：全部塞进 P02 无子 ID — 拒绝（无法挂字段/BR）。

3. **字段策略**  
   - 展开面板与安排详情表列**完整建 FD**（含同时授课/联动/场地类型/教室偏好）。  
   - 类型：教室偏好=多选下拉（与特殊课程纠错一致），非文本。  
   - tip 全文进 BR 或字段备注，禁止只写「见原型」。

4. **生成方式**  
   - 以对照清单手工/脚本生成完整 md，再写 docx（沿用 V3.1 字体约定）；禁止再跑「缩水批处理」覆盖本菜单。  
   - 改动单元格：宋体+TNR + highlight yellow。

5. **Latest Evaluation / 评教**  
   - PRD 写清「占位、模块未接入」→「未确认」；禁止写成已接生产评教。

## Risks / Trade-offs

- [漏界面] tip/弹窗仍有遗漏 → 实现前按下方 inventory 做 DOM/ID 勾选表，交付前对照用户点名的节点。  
- [与教师端 PRD 边界] 授课确认弹窗字段过细可能与「授课确认管理/教师端」重复 → 安排侧写入口+本页可见字段；深度流程引用其它菜单 PRD，不复制整章。  
- [docx 缩水] 旧 `write_docx_from_md_structure` 只写摘要表 → 本菜单须保证关键章节进 docx，或接受「以 md 为准、docx 全量同步」专门写入。

## Migration Plan

1. 覆盖写入 `…/开课安排20260902V2/`（md/docx/变更说明）。  
2. 对照短文路径仍指向 V2（内容已换）。  
3. 回滚：需从 git/备份恢复原 V2。

## Open Questions

- 无。覆盖策略已确认。
