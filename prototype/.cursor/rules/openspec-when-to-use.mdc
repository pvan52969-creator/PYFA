---
description: 何时走 OpenSpec：先判断再确认，小改直接做
alwaysApply: true
---

# OpenSpec 调用约定

OpenSpec（`/opsx:propose`、出 proposal/design/specs/tasks）**不是**每次改代码的必经步骤。

## 流程

1. 根据用户描述先判断：小改直接做，还是值得先提案。
2. **若倾向走 OpenSpec，必须先跟用户确认**，得到同意后再创建 change / 写产物。
3. 用户明确说「直接改」「别走 OpenSpec」「不用提案」→ 跳过 OpenSpec，直接改代码。
4. 用户明确说「出 OpenSpec」「先提案」「`/opsx:propose`」→ 可直接走提案，不必再问。

## 适合 OpenSpec

- 跨多页/多入口的产品或流程能力
- 有明确产品取舍，需要落档 why / 范围 / 非目标
- 分多步的大改（先定壳再改内部等），需要把第一步钉住
- 用户主动要求提案

## 不适合 OpenSpec（直接改）

- 小 bug、文案、样式微调
- 范围清晰、一两处就能改完，没有产品分叉
- 用户只说「改一下 / 修一下」且未提提案

## 确认话术（需要时）

简短说明判断即可，例如：

> 这更像跨入口的布局约定，建议先出 OpenSpec 把范围钉住；也可以直接改。你选哪个？
