## 1. 导航与上线

- [x] 1.1 侧栏「特殊开课」二级改为「开课计划 / 开课安排 / 开课名单」（data-page 仍用现有三页）
- [x] 1.2 去掉 `#page-course-offering-other`、`#page-course-special-offering-task`、`#page-course-special-offering-roster` 的 `hidden`
- [x] 1.3 `OFFERING_GROUPING_RETURN_LABELS` 改为「开课安排」「开课名单」

## 2. 开课计划

- [x] 2.1 计划页查询区 / 工具栏对齐专业开课：查询、重置、勾选、生效、删除；主按钮为「添加开课计划」，无「生成开课任务」
- [x] 2.2 列表去掉上课专业、上课批次、预置人数；保留课程库手动添加弹窗，新行不写 programmeKey / intake
- [x] 2.3 生效后可在开课安排看到；删除仅处理允许删除的行（对齐现有特殊开课删除边界，不扩大到已提交任务）

## 3. 开课安排

- [x] 3.1 安排页改为样式二步骤条（Course Setting / 师资安排），列表只含本学期已生效 `offeringType=other`
- [x] 3.2 筛选与列去掉专业 / 入学批次；安排教师进入 `#page-offering-grouping`，返回本页
- [x] 3.3 提交 / 退回行为保持「安排提交后才进名单」；草稿计划不出现在本页

## 4. 开课名单

- [x] 4.1 名单页查询区 / 列表壳对齐专业开课名单（管理名单入口）；去掉上课专业 / 上课批次筛和列
- [x] 4.2 仅展示已提交任务安排的特殊开课；空态提示先完成开课安排并提交
- [x] 4.3 分组页学生名单：只保留手工添加 / 移除；不出现按批次预置或按批次一键分配

## 5. 数据与联调

- [x] 5.1 启动不再 `migratePurgeSpecialOfferingPlanData` 清掉 other；不恢复旧的任务页演示注入
- [x] 5.2 走通：手动加课 → 生效 → 安排（两步 + 安排教师）→ 提交 → 名单手工加学生
- [x] 5.3 确认专业开课 / 选修开课列表与生成逻辑未被改动
