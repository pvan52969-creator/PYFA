## 1. 入口与弹窗壳

- [x] 1.1 专业开课计划工具栏「一键合班」右侧增加「ME开课」按钮
- [x] 1.2 新增弹窗/模态：课列表区 + 专业批次选择区 + 确认/取消
- [x] 1.3 打开时读取当前开课学期（默认展示学期或计划页筛选学期）

## 2. ME 课候选与范围规则

- [x] 2.1 课列表仅加载校选课程中 `electiveType === 'ME'`；不因 CHS/MAT/PHY 过滤课单
- [x] 2.2 实现专业批次候选：永久排除 CHS/MAT/PHY；再叠加课的 `excludedProgrammeKeys`
- [x] 2.3 实现「当前开课学期 + 入学批次 → YxSx」折算；命中 `excludedYears` 的批次默认不可选（无法折算则保守不可选并注释假设）
- [x] 2.4 选中课后按上述规则初始化批次勾选/禁用态，并允许在合法范围内调整

## 3. 确认落库

- [x] 3.1 确认时校验至少 1 个合法批次；否则提示不落库
- [x] 3.2 一门课多个批次合成一条 section（多 lineIds）；单批次同样一条
- [x] 3.3 同学期同课已存在批次则跳过；全部已存在则提示不新建
- [x] 3.4 写入后关闭弹窗并刷新专业开课计划列表

## 4. 验收

- [x] 4.1 走查：入口、ME 过滤、CHS/MAT/PHY 不出现、年级学期禁用、多批次一条任务、无批次拦截
- [x] 4.2 对照 spec 场景勾选完成；不实现 CHS/MAT/PHY 专用菜单
