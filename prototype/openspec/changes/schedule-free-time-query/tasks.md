## 1. 页面壳与导航

- [ ] 1.1 在 `index.html` 侧栏「课表查询」分组增加 `data-page="schedule-free-time"` 菜单「空闲时间查询」，并新增 `#page-schedule-free-time`（page-header + schedule-query-card + 导出条 + 列表），验证点击侧栏可进入该页
- [ ] 1.2 在 `app.js` 注册页面 id 与 `showPage` / 渲染入口（如 `renderScheduleFreeTimeQueryPage`），验证切换菜单时正确激活页面且离开其他排课页

## 2. 查询条件

- [ ] 2.1 实现筛选区：学年学期、周次起止（同一格）、星期多选、节次多选；布局符合一行最多 3 条件且按钮在第一行右侧；验证单周（起=止）与多周区间均可输入
- [ ] 2.2 实现对象混选：教师 / 学生 / 专业批次 / 场地（教室）多选控件（可复用密度页 multiselect 模式），验证可同时选中多类多个对象
- [ ] 2.3 实现查询 / 重置：未选任何对象时查询给出明确提示且不列出全校对象；重置后条件与列表回到默认/未查询态

## 3. 空闲计算与列表

- [ ] 3.1 实现筛选宇宙（周次×星期×节次）与按对象类型从演示排课数据取占用、求补集得空闲；连续节次合并为区间；验证全忙对象仍占一行且显示「无空闲」
- [ ] 3.2 渲染列表：序号、对象类型、对象、周次范围、空闲节次（过长省略+悬停全文）；验证一行一对象且行数与已选对象一致
- [ ] 3.3 结果卡去掉多余上留白（padding 与导出条对齐课表查询修复），验证导出条上方无大块空白

## 4. 导出与验收

- [ ] 4.1 实现按查询结果导出（CSV 或现有 list-export 演示），验证有结果时可导出、无结果时有合理提示
- [ ] 4.2 手测：混选教师+专业批次+教室，周次 3～5，限定星期一与部分节次，列表汇总可读；确认未改动课表查询 / 密度查询 / 排时间 / 排教室详情
