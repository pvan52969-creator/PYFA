---
description: 开课等二级菜单需求文档的版本递增与变更说明规范（禁止覆盖旧版）
globs: 参考文档/**/*
alwaysApply: true
---

# 二级菜单需求文档 · 版本与变更说明规范

适用于 `参考文档/2、开课管理/<一级菜单>/<二级菜单>/` 下按菜单拆分的 PRD（md / docx），以及后续同类模块文档。

## 目录结构（母文件夹 = 一级菜单）

```
参考文档/2、开课管理/
  开课设置/                 ← 一级菜单
    开课时间设置/           ← 二级菜单
    特殊课程设置/
  专业开课/                 ← 一级菜单
    开课计划/
    开课安排/
    开课名单/
  需求文档版本与变更说明规范.md
```

侧栏有新一级分组时，先建同名母文件夹，再在其下建二级菜单文件夹。

## 现行基线

- 当前已落地版本为 **V1**（命名示例：`开课时间设置20260803V1.md` / `.docx`）。
- **禁止覆盖**已有版本文件；再产出时必须升版。

## 命名

```
<二级菜单名称><YYYYMMDD><Vn>.md
<二级菜单名称><YYYYMMDD><Vn>.docx
```

- 日期：该版本**创建/发布**当日（不必与上一版同一天）。
- 版本号：同一菜单下单调递增 `V1` → `V2` → `V3`…（按菜单各自计数，不跨菜单共用序号）。

同目录并存多版本，例如：

```
开课设置/开课计划/   （示例路径）
专业开课/开课计划/
  开课计划20260803V1.md
  开课计划20260803V1.docx
  开课计划20260810V2.md
  开课计划20260810V2.docx
  开课计划20260803V1→20260810V2变更说明.md
  开课计划20260803V1→20260810V2变更说明.docx
```

## 升版时必须同时产出变更说明

模板（必须参考）：

`参考文档/0、模板/厦大马来分校本科教务系统——需求调整变更说明模板（简版）.docx`

变更说明文件命名：

```
<二级菜单名称><旧版日期><旧Vn>→<新版日期><新Vn>变更说明.md
<二级菜单名称><旧版日期><旧Vn>→<新版日期><新Vn>变更说明.docx
```

内容结构对齐简版模板：

1. **版本信息**：需求文档名称、上一版→本版、变更日期、填写人/确认人、确认状态  
2. **本次改了什么（总览）**：一行一事；变更类型仅用 **新增 / 修改 / 删除 / 澄清**；列：模块（菜单路径）、功能/位置、变更类型、调整内容、状态  
3. **前后对照**（可选）：总览说不清时再填  
4. **连带影响**（可选）：影响原型/开发/测试时再填  

填写口径：

- 模块：如 `开课管理 → 专业开课 → 开课计划`
- 功能/位置：按钮、字段、列表列、筛选项、抽屉分区/Tab、步骤、弹窗等
- 调整内容：一句话说清「从什么变成什么」；新增写「新增xxx」，删除写「删除xxx」

无实质需求变更、仅重跑脚本且内容与上一版一致时：**不要升版、不要覆盖**。

## Agent / 脚本行为

1. 生成或「再生成」某菜单 PRD 前：落到正确母文件夹 `一级菜单/二级菜单/`；扫描该文件夹取已有最高 `Vn`，新文件用 `V(n+1)` + 当日日期。  
2. **不得**用同名覆盖已有 `…V1.docx` / `…V1.md`。  
3. 升版时一并写变更说明（至少 md；docx 按简版模板结构生成或由脚本从模板填充）。  
4. 变更说明须对照上一版与本版差异（字段表、规则、界面、状态流等），禁止空表交差。  
5. 生成脚本默认拒绝覆盖；显式升版参数才写新文件。

相关脚本：

- `scripts/generate-submenu-prd-docx.py` — 菜单 PRD  
- `scripts/prd_submenu_fields.py` — 字段表数据  
- `scripts/generate-submenu-prd-change-note.py` — 变更说明（简版模板）
